-- Create a new database
CREATE DATABASE test_db;

-- Common SQL Data Types

-- INT              : Whole numbers
-- DECIMAL(m, n)    : Fixed-point numbers 
--                   m = total digits, n = digits after decimal
-- VARCHAR(l)       : Variable-length string of max length l
-- CHAR(l)          : Fixed-length string of length l
-- BLOB             : Binary Large Object (used for large binary data)
-- DATE             : Format YYYY-MM-DD
-- TIMESTAMP        : Format YYYY-MM-DD HH:MM:SS
